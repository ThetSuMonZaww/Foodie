import './style.scss'
import Typed from "typed.js";

var typed = new Typed('.element', {
    strings: ["Food ...", "Drink ..."],
    typeSpeed: 30,
    loop: true,
    backSpeed: 30,
    backDelay: 3000
});
